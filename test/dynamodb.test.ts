// import * as yup from 'yup';
// import { Table } from '../src/dynamodb';

// const table = new Table('test1', yup.object({
//     id: yup.string(),
//     name: yup.string(),
// }))

describe.skip('dynamodb', () => {
    it('putItem', async () => {
        // const data = await table.putItem({
        //     id: "test",
        //     name: "test",
        // })
        // expect(data.id).toEqual(2);
    });
});
